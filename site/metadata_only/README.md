# Metadata Only (non-runtime)

Questa cartella contiene file e strumenti non necessari al runtime del sito deployato.

Contenuto tipico:
- report/import/cache/timeline
- script di build/manutenzione
- cartelle di supporto deploy locale
